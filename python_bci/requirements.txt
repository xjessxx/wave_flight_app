# Motor Imagery BCI System - Python Dependencies
# Install with: pip install -r requirements.txt

# Core Scientific Computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0

# EEG Hardware & Signal Processing
brainflow>=5.0.0
mne>=1.0.0

# Machine Learning
scikit-learn>=1.0.0

# Bluetooth Communication (optional - may require system dependencies)
# On Linux: sudo apt-get install libbluetooth-dev
# On Mac: Should work out of the box
# On Windows: Bluetooth library is challenging to install, skip for now
# pybluez>=0.23; sys_platform != 'win32'

# Data Logging
h5py>=3.0.0
